{
	"type": "module",
	"name": "@lespantsfancy/chord",
	"version": "0.1.17",
	"description": "",
	"main": "dist/main.js",
	"scripts": {
		"build": "babel src --out-dir dist --copy-files",
		"build-publish": "npm version patch && npm run build && npm publish",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Matt Nawrocki",
	"dependencies": {
		"react": "^18.2.0",
		"uuid": "^9.0.0"
	},
	"devDependencies": {
		"@babel/cli": "^7.22.9",
		"@babel/core": "^7.22.9",
		"@babel/preset-env": "^7.22.9",
		"@babel/preset-react": "^7.22.5"
	}
}
